-- =========================================
-- =                OPTIONS                =
-- =========================================

controls = {
    inventory = keys.I,
    move_up = keys.Z,
    move_left = keys.Q,
    move_down = keys.S,
    move_right = keys.D,
    sprint = keys.LShift,
    pickup = keys.F,
}