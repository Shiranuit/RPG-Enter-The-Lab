scythe_func = {
    function(x)
        return math.cos(x / 40) * 10
    end,
    function(x)
        return math.sin(x / 20) * 2
    end,
    function(x)
        return math.sin(x / 5) * math.cos(x / 5) * 5
    end
}